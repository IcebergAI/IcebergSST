"""Ownership configuration: groups, routing rules, and response targets (#146).

The *policy* above finding triage. What it decides about an individual finding —
who owns it, when it is due — lives in :mod:`iceberg_api.findings.ownership`.
"""
